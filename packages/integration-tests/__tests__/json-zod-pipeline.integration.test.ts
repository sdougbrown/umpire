import { describe, it } from 'bun:test'

describe('json + zod pipeline', () => {
  it.todo(
    'covers fromJson()/toJson() schema round-trip feeding umpire() availability into deriveSchema()/deriveErrors()',
    () => {},
  )
})
