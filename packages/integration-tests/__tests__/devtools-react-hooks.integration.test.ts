import { describe, it } from 'bun:test'

describe('devtools/react hooks lifecycle', () => {
  it.todo(
    'covers register-on-render and unregister-on-unmount behavior for useUmpireWithDevtools()',
    () => {},
  )
})
